{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DirectorOutput",
  "type": "object",
  "required": ["shots"],
  "additionalProperties": false,
  "properties": {
    "shots": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["shot_id", "scene_id", "dramatic_beat", "emotion", "shot_size", "subject", "edit_intent"],
        "properties": {
          "shot_id": { "type": "string" },
          "scene_id": { "type": "string" },
          "dramatic_beat": { "type": "string", "description": "Chinese or primary beat for humans" },
          "dramatic_beat_en": {
            "type": "string",
            "description": "English dramatic beat for generation prompts (preferred in compiler)"
          },
          "emotion": {
            "type": "object",
            "properties": {
              "primary": { "type": "string" },
              "intensity": { "type": "number" }
            },
            "required": ["primary"]
          },
          "shot_size": {
            "type": "string",
            "enum": ["EWS", "WS", "FS", "MS", "MCU", "CU", "ECU", "INSERT"]
          },
          "subject": { "type": "string", "description": "Chinese or primary subject for humans" },
          "subject_en": {
            "type": "string",
            "description": "English subject for generation prompts (preferred in compiler)"
          },
          "whose_pov": { "type": "string" },
          "edit_intent": { "type": "string" },
          "camera_draft": { "type": "string" },
          "linked_dialogue": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    }
  }
}
